# xmup

## Requirements

- python 3.6+

## Install

```shell
# Linux and Mac
pip3 install xmup
#  windows
pip install xmup
```

## Use

Open a shell (terminal, bash, cmd, ...) and run:

```shell
xmup
```
